@model Application.ViewModels.GendersViewModel.SaveGenderViewModel
@{
    bool editMode = Model.Id == 0 ? false : true;
    ViewData["Title"] = "Create Gender";
}

<div class="row">
    <div class="col-4 offset-4">
        <div class="card">
            <div class="card-header bg-success text-light">
                <h4 class="text-center  text-light">@(editMode ? $"EDITING THE GENDER: {Model.Name}" : "CREATING A GENDER")</h4>
            </div>

            <div class="card-body">
                <form method="post" asp-controller="Gender" asp-action="@(editMode ? "Update":"Create")">
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">Name</label>
                        <input asp-for="Name" type="text" class="form-control"
                            placeholder="Write the name of the gender" autocomplete="off" />
                        <span class="text-danger" asp-validation-for="Name"></span>
                    </div>
                    <input type="hidden" asp-for="Id" />
                    <button class="btn btn-primary float-end ms-1" type="submit">Save Gender</button>
                    <button class="btn btn-success float-end" type="submit" asp-controller="Gender" asp-action="Index">Return</button>
                </form>
            </div>

        </div>
    </div>
</div>